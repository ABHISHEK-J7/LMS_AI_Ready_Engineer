# LMS_Database

Schemas live as Mongoose models in `LMS_Backend/src/models`. Seed data + the default 10-module curriculum are in `LMS_Backend/src/seed`. This folder holds DB docs, migration notes, and exported fixtures.
